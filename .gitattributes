# Normalize line endings to LF in the repo. This project is authored on WSL;
# the Windows editing tools write CRLF, so without this every edit would churn
# line endings. `text=auto eol=lf` stores LF on commit regardless of the working
# tree, keeping diffs content-only.
* text=auto eol=lf
